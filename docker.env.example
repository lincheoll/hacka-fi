# =============================================================================
# ROOT ENVIRONMENT CONFIGURATION
# =============================================================================
# This file contains shared environment variables used across all workspaces
# and Docker Compose orchestration. Each workspace has its own .env.example
# file for workspace-specific variables.
#
# Usage:
# 1. Copy this file to .env: cp .env.example .env
# 2. Update values for your environment
# 3. Configure workspace-specific variables in each workspace
#
# Workspaces:
# - apps/web/.env.example (Next.js frontend variables)
# - apps/api/.env.example (NestJS backend variables) 
# - contracts/.env.example (Smart contract deployment variables)
# =============================================================================

# Environment
NODE_ENV=development

# Service Ports (for Docker Compose)
API_PORT=3001
WEB_PORT=3000
DATABASE_PORT=5432
REDIS_PORT=6379

# Database (Shared across API and Docker)
POSTGRES_DB=hacka_fi
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password

# Shared Blockchain Configuration
KAIA_CHAIN_ID=1001
RPC_URL=https://public-en-kairos.node.kaia.io

# Deployed Contract Addresses (Updated after deployment)
# These are shared between API and Web workspaces
HACKATHON_REGISTRY_ADDRESS=0x0000000000000000000000000000000000000000
PRIZE_POOL_ADDRESS=0x0000000000000000000000000000000000000000

# Cross-Service Configuration
CORS_ORIGIN=http://localhost:3000