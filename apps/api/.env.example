# Database Configuration
# SQLite for development (default)
DATABASE_URL="file:./dev.db"

# PostgreSQL for production (uncomment and update values)
# DATABASE_URL="postgresql://username:password@localhost:5432/hacka_fi?schema=public"

# Environment flag to determine database provider
DB_PROVIDER="sqlite"